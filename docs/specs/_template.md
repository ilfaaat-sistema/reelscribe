# ТЗ NN. Название улучшения

**Статус:** идея | ТЗ | в работе | готово

## Проблема / цель

Что не так сейчас или какую новую ценность даёт улучшение.

## Пользовательский сценарий

Как пользователь будет пользоваться фичей, шаг за шагом.

## Функциональные требования

Что именно делаем — конкретный список поведения.

## Затрагивается

- Экраны/компоненты: …
- Таблицы БД / edge-функции: …

## Вне рамок

Что сознательно НЕ входит в это ТЗ.

## Реализация (готовый план исполнения — сессия модуля НЕ планирует заново)

- **Модель сессии:** sonnet | opus (сложный модуль/оркестрация) | haiku (чистая механика) — с обоснованием одной фразой.
- **Команда запуска:** `claude --model <модель> --permission-mode acceptEdits` (авторежим правок).
- **План оркестрации по этапам:**
  1. Последовательный фундамент (например: миграция → типы → build).
  2. Параллельные куски субагентами — для каждого: название, СПИСОК ЕГО ФАЙЛОВ (куски не пересекаются по файлам!), модель субагента.
  3. Сборка/интеграция и что верифицировать после каждого этапа.

## Критерии приёмки

Проверяемые условия, при которых улучшение считается готовым.
