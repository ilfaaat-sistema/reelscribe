# Зависимости API-сервиса (Vercel Services, backend/ → app.main:app).
# Только то, что реально импортируется по цепочке app/main.py → app/api/*.py →
# app/core/*.py, app/models/*.py, app/services/*.py, app/pipeline/normalize.py,
# включая ленивые импорты внутри функций (app/api/sessions.py:retry_jobs и
# app/services/import_service.py дергают app/pipeline/actions_trigger.py, который
# лениво импортирует httpx — поэтому httpx нужен и API, не только воркеру).
# Тяжёлые пакеты пайплайна (скачивание, ASR, перевод) — в requirements-worker.txt,
# их ставит только воркер (GitHub Actions / локальный Mac), не Vercel.
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
# >=2.19.0 обязательно: в более старых postgrest метод .order() не умеет писать nullslast,
# и сортировка по просмотрам/залётности ставит записи с пустыми метриками в начало.
supabase>=2.19.0
openpyxl>=3.1.2
# >=0.28.1 обязательно: google-genai 1.47.0 (ниже) требует эту версию httpx, со старым
# пином (0.27.0) пакеты вместе не ставятся (конфликт разрешения зависимостей pip).
httpx>=0.28.1
# Радар (ТЗ 08): один вызов Gemini на разбор рилса — расшифровка речи + видеоанализ,
# прямо в API-функции Vercel (без Whisper/ffmpeg/воркера). Сверено с фактически
# установленной в venv версией 1.47.0.
google-genai>=1.20
