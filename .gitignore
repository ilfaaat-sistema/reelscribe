.env
secrets/
*cookies*.txt
.instaloader-session*
.venv/
__pycache__/
*.pyc
*.pyo
.DS_Store
/tmp/
node_modules/
dist/
*.egg-info/
.pytest_cache/
.mcp.json

# личные данные
Копия reels_list.csv
*.csv
# выгрузки Instagram (переписки, сохранённое) — десятки МБ личного, в git не кладём
your_instagram_activity*/
Мои инстаграммы/
instagram-*-*/
message_*.json
.vercel
.env*

# постоянные git worktree модулей (см. скилл module-flow) — сами по себе в репозиторий не идут
worktrees/

# снимки scripts/health.py (датчик состояния сервиса) — локальные, не для git
.health/

# служебные файлы supabase CLI (линковка проекта)
supabase/.temp/

# сырые ответы scripts/probe_business_discovery.py — локальные пробы, не для git
.probe/

# видео не храним (железное правило проекта) — материалы разбора держим только текстом
*.mp4
